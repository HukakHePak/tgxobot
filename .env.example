# Copy this to `.env` or export variables in your environment before running compose
# Required for both dev and prod compose (no defaults in compose files):
PORT=3001
NGINX_PORT=8080

# Optional: BOT_TOKEN=your_bot_token_here
