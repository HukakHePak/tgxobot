networks:
  tgxonet:
    driver: bridge

services:
  server:
    build:
      context: ./server
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - .env
    networks:
      - tgxonet

  client:
    build:
      context: ./client
    volumes:
      - ./client:/app
      - /app/node_modules
    env_file:
      - .env
    networks:
      - tgxonet

  nginx:
    image: nginx:1.25-alpine
    volumes:
      - ./nginx/nginx.dev.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "${NGINX_PORT}:80"
    depends_on:
      - client
      - server
    networks:
      - tgxonet
